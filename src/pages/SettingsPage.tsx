// pages/SettingsPage.tsx
const SettingsPage = () => (
  <div>
    <h1 className="text-2xl font-bold text-gray-900">Settings</h1>
    <p className="mt-1 text-sm text-gray-500">Manage application settings.</p>
    {/* Add settings sections here — each section is its own feature component */}
  </div>
)
export default SettingsPage
